import { Card } from "../components/ui/Card";
import { PageHeader } from "../components/layout/PageHeader";

export const SettingsPage = () => (
  <Card>
    <PageHeader title="Settings" subtitle="Future configuration (taxes, integrations, printers)." />
  </Card>
);
