# Audit Logging

Audit logs capture:
- Inventory adjustments
- Price overrides
- Stock overrides
- Refund/void approvals
- Receipt reprints

Logs are stored in `auditLogs` and visible to managers (shop-scoped) and admins (global).
